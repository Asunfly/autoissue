# 通用 Skill（Browser-use）

## 核心流程
1. 打开指定的 Issue 模板页面。
2. 如果要求登录，等待用户登录完成。
3. 填写标题与表单字段（按标签定位）。
4. 如有附件，上传到合适的描述或附件位置。
5. 等待指定秒数供人工确认。
6. 根据指示执行提交或停止。
7. 提交后停留在结果页面，记录 URL。

## 操作细节
- 优先使用可见标签定位输入框/下拉框。
- 下拉字段从给定选项中选择最匹配项。
- 必填字段为空时，不要强行提交。
